QUnit.module("calculate Discount tests", () => {

    QUnit.test("Expect _____", (assert) => {
        // 1. Arrange
        const order = undefined; //...;
        const expected = undefined; //...;

        // 2. Act
        const actual = calculateDiscount(order);

        // 3. Assert
        assert.equal(actual, expected);
    });

});


